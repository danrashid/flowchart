g(transform="translate(#{x},#{y})")
  rect(x="50" y="50" width="200" height="100" stroke="#000000" fill="#FFFFFF")
  text(font-family="Helvetica Neue")
    each line, i in text
      tspan(x="150" y=85 + (i * 20) text-anchor="middle")= line
