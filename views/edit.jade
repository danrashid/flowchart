extends layout

block content
  h1= title

  form(method="post")
    p
      label
        | Chart name
        br
        input(type="text" name="name" required value=chart.name)
    p
      label
        | Markup
        br
        textarea(name="markup" rows="10" required)= chart.markup
    p
      button(type="submit") Save chart
